name = "paigack/injuryrisk"
version = "0.1.0"
readme = "README.mbt.md"
repository = "https://github.com/PaiGack/moonbit-injuryrisk"
license = "Apache-2.0"
keywords = ["injury-risk", "rule-engine", "sports-science", "sliding-window"]
description = "A lightweight rules-based engine for training load, pain, sleep, and subjective fatigue risk stratification."

options(
  "preferred-target": "native"
)
